{
  "name": "@stoplight/ref-resolvers",
  "version": "0.0.0",
  "description": "Set of utilities for resolving external json references",
  "files": [
    "**/*"
  ],
  "engines": {
    "node": ">=8.3.0"
  },
  "main": "src/index.ts",
  "types": "dist/index.d.ts",
  "homepage": "https://github.com/stoplightio/ref-resolvers",
  "bugs": "https://github.com/stoplightio/ref-resolvers/issues",
  "author": "Stoplight <support@stoplight.io>",
  "repository": {
    "type": "git",
    "url": "https://github.com/stoplightio/ref-resolvers.git"
  },
  "scripts": {
    "build": "sl-scripts build",
    "commit": "git-cz",
    "lint": "tslint -p .",
    "lint.fix": "yarn lint --fix",
    "release": "sl-scripts release",
    "release.dryRun": "sl-scripts release --dry-run --debug",
    "test": "jest",
    "test.prod": "yarn lint && yarn test --coverage --no-cache",
    "test.update": "yarn test --updateSnapshot",
    "test.watch": "yarn test --watch"
  },
  "dependencies": {
    "node-fetch": "^2.6.0"
  },
  "devDependencies": {
    "@stoplight/scripts": "^7.0.3",
    "@types/jest": "^24.0.18",
    "@types/node-fetch": "^2.5.2",
    "jest": "^24.9.0",
    "ts-jest": "^24.1.0",
    "tslint": "^5.20.0",
    "tslint-config-stoplight": "^1.3.0",
    "typescript": "^3.6.3"
  },
  "release": {
    "extends": "@stoplight/scripts/release"
  }
}
